import 'package:flutter/material.dart';

class CurrentMealPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Current Meal"),
      ),
    );
  }
}
